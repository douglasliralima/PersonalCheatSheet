#include <stdio.h>
#include <stdlib.h>
    int main(void)
    {
        int a=2;
        if(a==1)
            puts("Iaew o/");
        else
            puts("Se é bichão memo"); //Os acentos estão dando pau, acho que deve ser por estar em ASCII, tenho que saber como mudar para UTF-8
            //Colocar um + e a váriavel no puts "puts("merda"+a) como no JAVA não funciona, lembre, coisas diferentes
        return 0;
    }
